import { PlaceholderPage } from './PlaceholderPage';

export function LogInPage() {
  return (
    <PlaceholderPage
      title="Log in"
      description="Login flow will connect here when the next Figma screen is ready."
    />
  );
}
